# 解析ダイアログ（F-51・F-52・F-53）

> ステータス: 🟢 枠組み・概要モード（§2〜§4・§8）は実装済み（2026-08-20、T-096）。
> 🔴 指標詳細モードの中身（§5.1線グラフ・§5.2連続年数・§5.3汎用8指標）は未実装
> （プレースホルダーのみ。T-097・T-098 待ち）
> **独立した URL を持つ「画面」ではない。** [`/`](./search-page.md) と
> [`/portfolio`](./portfolio-page.md)（T-081）の両方から開けるモーダル状態
> （[ADR-0014](../../../adr/0014-analysis-dialog-url-state.md) が URL の正）
> 対応する機能ID: [F-51](../../../01_requirements/features.md)（ダイアログ）・
> [F-52](../../../01_requirements/features.md)（配当推移の折れ線）・
> [F-53](../../../01_requirements/features.md)（連続非減配年数のリスト）
> 出典: [design_mock](../../../design_mock/README.md) §3「Analysis Dialog」

## 変更履歴

- **2026-08-16**: 新規作成（T-073）。試作のレイアウトを反映しつつ、
  「評価」列（判定文言）を不採用にする等、投資助言色を排除した（§4.3）
- **2026-08-20**（T-096）: `frontend/components/Dialog.tsx`（フォーカストラップ・
  `aria-modal`・Escape・背景クリック・フォーカス復帰）＋概要モード（§2〜§4）を実装。
  指標詳細モード（§5）は「← 指標一覧へ戻る」ヘッダー＋現在値・スコアのみで、
  中身（§5.1〜§5.3）はプレースホルダー（T-097/T-098 に委ねる）。§4.2 の比較表の行は
  stretched button 化し、`onOpenMetric` で `?metric=` へ遷移する導線を実装済み
- **2026-08-20**（T-096 fe-reviewer レビュー是正、CR-1〜CR-8 + NEW-1・NEW-2）: 上記実装への
  2巡目レビューで検出した齟齬を修正。指標比較表のスコア列に `<ScoreBar>` を追加
  （判定不能時はバー無し。CR-1）、比較表に「›」装飾列（`aria-hidden`）を追加（CR-2）、
  ダイアログ内エラー表示に `role="alert"` を追加（CR-3）、ダイアログヘッダーに銘柄コードを
  併記（CR-4。§3 が指定する `--color-text-secondary` を使用。一度は一覧行と同じ
  `--color-text-tertiary` のクラスを流用してしまい `NEW-2` として指摘されたが、ユーザー
  決定で実装を設計書どおり `--color-text-secondary` に修正した）、概要モードのヒーロー行を
  `.dialog-summary` の2カラムグリッド（総合スコアカード左・レーダーチャート右）にし、
  §3 のレイアウト図が元々要求していた構成と実装を一致させた（CR-5）、概要⇄指標詳細モード
  切り替え時のフォーカス管理を追加（詳細→概要は見出しへ、概要→詳細は「← 指標一覧へ戻る」
  ボタンへ。CR-8）。CR-6（フォーカスリング幅 `3px` を design-tokens.md §6 の直値禁止の
  対象外とする根拠コメントを追加）・CR-7（`App.tsx` のダイアログ開閉ハンドラを
  `useCallback`/`useMemo` 化）も同時に是正済み

---

## 1. 概要

**1銘柄の採点結果を全指標ぶん見せる、モーダルダイアログ。**
検索一覧（`/`）・ポートフォリオ（`/portfolio`）の行クリックから開く共通部品。

**この画面がしないこと:**

- スコアの計算（すでに確定した `ScoringResponse` を表示するだけ）
- 複数銘柄の比較（F-33。別画面の役割）
- 投資判断の提示（「買い」「割安」等の評価文言を出さない。§4.3）
- ポートフォリオへの追加・保有数量の編集（`/portfolio` 側の責務）

**2つのモードを持つ**（既存の `ListPage` の「解析結果」インラインセクションを
モーダル化したもの）。

- **概要モード**（既定）: 総合スコア・レーダーチャート・10指標の比較表
- **指標詳細モード**: 1指標にドリルダウンした詳細（線グラフ／連続年数リスト／汎用）

## 2. URL・開閉

**[ADR-0014](../../../adr/0014-analysis-dialog-url-state.md) が正。** 要点だけ再掲する。

| 状態                 | URL                            |
| :------------------- | :----------------------------- |
| 閉                   | `?code=` 無し                  |
| 開（概要モード）     | `?code=<コード>`               |
| 開（指標詳細モード） | `?code=<コード>&metric=<キー>` |

- 行クリックで開く場合は常に**概要モードから始まる**（`metric` を付けない）
- `?code=X&metric=Y` への直リンクは**指標詳細から始まってよい**（共有・ブックマークの利点）
- ✕ ボタン・背景（scrim）クリック・Escape キー → `code` と `metric` の両方を外す（閉じる）
- 「← 指標一覧へ戻る」 → `metric` だけを外す（`code` は残る。ダイアログは閉じない）
- **背景クリックは「指標一覧へ戻る」の代わりにならない。** 指標詳細モードで背景を
  クリックするとダイアログごと閉じる（試作の挙動どおり）

## 3. レイアウト（概要モード）

```
┌──────────────────────────────────────────────────────┐
│ トヨタ自動車  7203 · 東証プライム              ✕        │
├──────────────────────────────────────────────────────┤
│ ┌─────────────────┐  ┌─────────────────────────┐    │
│ │ 総合スコア         │  │ 指標プロファイル（各 0〜10）│    │
│ │   62 /100 点       │  │                            │    │
│ │  ▓▓▓▓▓▓░░░░        │  │      （レーダーチャート）    │    │
│ │  有効指標 8/10      │  │                            │    │
│ │  ─────────         │  └─────────────────────────┘    │
│ │  株価  3,142 円     │                                  │
│ │  配当利回り 3.18%   │                                  │
│ │  PER   14.2 倍      │                                  │
│ │  PBR   — データなし │                                  │
│ │  ─────────         │                                  │
│ │  ☐ 実績配当性向を   │                                  │
│ │    採点に使う        │                                  │
│ └─────────────────┘                                  │
├──────────────────────────────────────────────────────┤
│ #  指標              値       スコア        備考      ›  │
│ ── 10行、§4.2 参照 ──────────────────────────────────  │
├──────────────────────────────────────────────────────┤
│ 各指標の行を押すと、年次推移などの詳細を表示します。      │
└──────────────────────────────────────────────────────┘
```

- scrim: `rgba(6,8,12,0.74)` ＋ `backdrop-filter: blur(3px)`（[`<Dialog>`](../components.md)）
- カード: `max-width: 940px`、`--radius-2xl`、`--shadow-dialog`（このアプリで唯一の影）
- ヘッダー: 銘柄名（`--font-size-lg`/700）＋コード・市場区分（`--font-mono` `--color-text-secondary`）
- ヒーロー行は2列グリッド（左270px固定・右は残り幅）

## 4. 概要モードの表示仕様

### 4.1 総合スコアカード（左）

| 項目               | 表示                                                                                            |
| :----------------- | :---------------------------------------------------------------------------------------------- |
| 総合スコア         | `--font-size-hero`（52px）/700 `--font-mono` ＋「/`maxTotalScore`」                             |
| 進捗バー           | [`<ScoreBar>`](../components.md)。`value=totalScore`, `max=maxTotalScore`                       |
| 有効指標           | 「有効指標 `effectiveMetricCount`/`totalMetricCount` 指標で算出」（`--color-text-tertiary`）    |
| 株価               | `<Money sen={priceSen} />`                                                                      |
| 配当利回り         | ⑩の値（`formatMetricValue`）                                                                    |
| PER                | 数値＋「倍」。`null` なら「— データなし」                                                       |
| PBR                | 同上                                                                                            |
| 実績配当性向トグル | 既存の「実績配当性向を採点に使う」チェックボックス（`useActualForScoring`）を**この位置に移設** |

- PER/PBR が近似値（取り込み由来）の場合、`perSource`/`pbrSource` の文言
  （`multipleSourceText`。既存 `format.ts` を流用）を併記する
- **「— データなし」と「0」を混同しない。** `null` は必ず `—`
- **指標カスタマイズ実装後（T-101）は、総合スコアの下に「指標の選択・基準値が異なる
  相手とは総合点を単純比較できません」の注記を追加する。**
  [ADR-0012](../../../adr/0012-indicator-customization-scaling-and-denominator.md)
  §決定D-3 が本画面への明記を必須としている（2026-08-17、T-088レビューで
  反映漏れが発覚し追加）。**指標カスタマイズ未実装の間（全ユーザー共通の
  `score_cards.total_score` を表示している間）は不要**（比較不能な状態が存在しないため）

### 4.2 指標比較表

| 列     | 内容                                                                                                  |
| :----- | :---------------------------------------------------------------------------------------------------- |
| #      | `metric.number`（①〜⑩）                                                                               |
| 指標   | `metric.label`                                                                                        |
| 値     | `formatMetricValue(metric.value, metric.unit, ...)`。`null` は `—`                                    |
| スコア | [`<ScoreBar>`](../components.md)（`value=score, max=10`。判定不能時はバー無し）＋数値。判定不能は `—` |
| 備考   | `reasonText(unavailableReason)`。③行だけ `payoutRatioBreakdownText` を追記                            |
| ›      | 装飾のみ（`aria-hidden`）。行全体が §4.4 の stretched button                                          |

**列は既存 `MetricTable.tsx` の構成（#・指標・値・スコア・備考）を踏襲し、
スコア列に `<ScoreBar>` を追加しただけ。** 備考列のロジック（③の内訳併記等）は
変更しない。

### 4.3 ⚠️ 「評価」列は採用しない（試作からの意図的な逸脱）

design_mock の比較表には「評価」列があり、各行に短い判定文言
（例:「安定して増配」「バランス良好」「業種平均をやや上回る」）を出す。

**この列は不採用とする。** 理由:

- `CLAUDE.md`「投資判断そのものを自動化・推奨する機能は作らない」という方針に対し、
  数値をもとに自動生成された評価文言は**投資判断の提示に近い**
- 既存実装（`MetricTable.tsx`）にもこの種の文言は無く、これまで一貫して
  「数値とスコアだけを見せ、解釈はユーザーに委ねる」設計を保ってきた
  （スコアに緑赤を使わない、Delta に色だけで意味を持たせない、等と同じ立場）
- 「評価」列を削っても、スコア（0〜10の数値＋バー）自体が十分な情報を持つ

## 5. 指標詳細モード

「← 指標一覧へ戻る」ボタン ＋ 指標名 ＋ 現在値・スコアの後、指標ごとに3種類のいずれかを表示する。

### 5.1 線グラフ（①増配率（5年CAGR）のみ。F-52）

- SVG の折れ線グラフ（Recharts。[components.md](../components.md) §7 決定）
- **年度は固定8年ではなく、その銘柄に登録されている `dividend_records` の
  全年度**（昇順）。予想（`kind = 'forecast'`）の年度は年ラベルに「（予想）」を付ける
- 縦軸グリッド線、各点の上に金額ラベル、下に年度ラベル
- グラフの下に表（年度／1株配当／前年比）。前年比は `▲ +N円` / `▼ -N円` / `－ 据置`
  （色＋glyph＋文言。色だけに依存しない。`.claude/rules/frontend.md`）
- **配当データが1年分しか無い（前年比を計算できない）場合、その行の前年比は `—`**
  （`±0円` にしない。データ欠損と「変化なし」を区別する）

### 5.2 連続年数リスト（②連続非減配年数のみ。F-53）

- 縦並びのリスト。1行＝1年度。年度・`▲`/`－`/`▼` glyph＋色・状態語
  （増配／据置／減配）・金額・前年差
- 末尾に要約行:「減配のない状態が **`consecutiveYears`年** 継続中です。」
- 対象年度は最大18年遡及（`consecutive-years-scoring.md` の定義に合わせる）

### 5.3 汎用（残り8指標。③④⑤⑥⑦⑧⑨⑩）

- 計算式を等幅フォントのインセットブロックで表示
- **条件／点数の表は [評価基準タブ](./criteria-tab.md) と同じ `bands.ts` から描画する。**
  ハードコードしない・二重管理しない（`criteria-tab.md` §3 と同じ規約）
- ③のカードには、予想／実績それぞれの値・スコア・採用元を併記する
  （`payoutRatioBreakdownText` を流用）

## 6. 読み込み中・エラー時の表示

- ダイアログを開いた直後、`ScoringResponse` の取得が終わるまでは
  ヘッダー（銘柄名）だけを先に出し、本体は `<Skeleton>` にする
  （既に選択済みの銘柄名は一覧のデータから分かるため、タイトルは即座に出せる）
- 取得に失敗した場合、ダイアログ内に `role="alert"` でエラーを表示し、
  「一覧から選び直してください」等の次の行動を示す。ダイアログは閉じない
  （閉じると URL の `?code=` が残ったままエラーの手がかりが消える）

## 7. データ取得（API）

✅ **2026-08-17、[company-api.md](../../api/company-api.md)（T-077）で設計確定 →
2026-08-20、T-096 着手前のブロッカー解消で実装完了。**

- `priceSen`/`per`/`pbr`（数値）は `GET /api/companies/:code` の `ScoringResponse` に追加済み
  （概要は取得時に一括で返る）
- 配当の年次履歴（①線グラフ・②連続年数リストで共用）は
  **`GET /api/companies/:code/dividends` を別エンドポイントにした。**
  指標詳細を開いたときだけ追加取得し、1指標を見るためだけに概要のペイロードを重くしない
  （検討した2案のうち、これを採用）

## 8. アクセシビリティ

- ダイアログは `role="dialog"` `aria-modal="true"` `aria-labelledby="<銘柄名の要素ID>"`
- **フォーカストラップ。** 開いたら最初のフォーカス可能要素（✕ボタン）へ移動し、
  Tab はダイアログ内で循環する
- Escape キーで閉じる（概要・詳細どちらのモードでも）
- 比較表の行・「← 指標一覧へ戻る」は [search-page.md](./search-page.md) §4.1 と同じ
  **stretched button パターン**（`div` + `onClick` にしない）
- 閉じたらフォーカスを元の呼び出し元（一覧の行ボタン）に戻す

## 9. 受入基準

- ✅ 一覧の行を Enter/Space または クリックで開くと、概要モードで開く（`?code=` のみ）
- ✅ 指標行を開くと `?metric=<キー>` が付き、詳細モードに切り替わる
- ✅ 「← 指標一覧へ戻る」を押すと `?metric=` だけが外れ、`?code=` は残る
- ✅ ✕ ボタン・背景クリック・Escape のいずれでも `?code=` と `?metric=` の両方が外れる
- ✅ `?code=X&metric=roeAverage` に直接アクセスすると、指標詳細モードで開く
- ✅ PBR が `null` の銘柄で「— データなし」と表示され、`0` は出ない
- ✅ 指標カスタマイズ実装後、総合スコアの下に総合点の比較不能を示す注記が表示される
- ✅ 「評価」列は比較表に存在しない（§4.3）
- ✅ 判定不能（`score: null`）の行にスコアバーが表示されず、`—` が出る
- ✅ ①の線グラフで、配当が1年分しかない年度の前年比が `—` になり `±0円` にならない
- ✅ ダイアログを開くとフォーカストラップが働き、Tab がダイアログ外に出ない
- ✅ Escape キーでダイアログが閉じ、フォーカスが呼び出し元の行ボタンに戻る
- ✅ `frontend/` に色・サイズの直値が無い（[design-tokens.md](../design-tokens.md) §6）

## 10. 関連ドキュメント

- [search-page.md](./search-page.md) — このダイアログを開く画面（T-072）
- [portfolio-page.md](./portfolio-page.md) — 同じダイアログを開くもう1つの画面（T-081）
- [criteria-tab.md](./criteria-tab.md) — 汎用詳細の条件／点数表の出所（`bands.ts`）
- [ADR-0014](../../../adr/0014-analysis-dialog-url-state.md) — URL 設計（正）
- [components.md](../components.md) — `<Dialog>` `<ScoreBar>` `<Money>` `<Skeleton>` の仕様
- [consecutive-years-scoring.md](../../logic/consecutive-years-scoring.md) — ②の定義
- [company-api.md](../../api/company-api.md) — この画面が呼ぶ API（T-077 で拡張）
- [design-mock-alignment.md](../../../03_tasks/design-mock-alignment.md) — 反映計画（T-073）

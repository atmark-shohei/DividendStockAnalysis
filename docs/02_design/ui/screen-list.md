# 画面一覧・遷移図

> ステータス: 🟡 draft

## 画面一覧

| ID   | 画面                 | パス                   | 優先度 | 詳細仕様                                             |
| ---- | -------------------- | ---------------------- | ------ | ---------------------------------------------------- |
| S-01 | ダッシュボード       | `/`                    | P0     | [pages/dashboard-page.md](./pages/dashboard-page.md) |
| S-02 | 銘柄スクリーニング   | `/screener`            | P0     | 未作成                                               |
| S-03 | 銘柄詳細             | `/stocks/[code]`       | P0     | 未作成                                               |
| S-04 | ウォッチリスト       | `/watchlist`           | P0     | 未作成                                               |
| S-05 | ログイン             | `/login`               | 保留   | [pages/login-page.md](./pages/login-page.md)         |
| S-06 | 設定                 | `/settings`            | P2     | 未作成                                               |
| S-07 | 取り込みエラー一覧   | `/admin/ingest-errors` | P2     | 未作成                                               |
| S-08 | データ入力・解析タブ | `/input`               | P0     | 未作成                                               |
| S-09 | 評価基準タブ         | `/criteria`            | P0     | [pages/criteria-tab.md](./pages/criteria-tab.md)     |
| S-10 | 保存済み銘柄タブ     | `/saved`               | P1     | 未作成                                               |

> ⚠️ **2026-07-26: この画面構成は要再検討。**
> OneDrive「銘柄スカウティング」から取り込んだ設計書は**タブ型の単一画面アプリ**
> （データ入力・解析 / 評価基準 / 保存済み）を前提にしている。一方 S-01〜S-07 は
> 取り込み前に仮置きした**画面遷移型**の構成で、両者は別物である。
>
> S-08〜S-10 が取り込んだ実設計、S-01〜S-07 は仮置き。**どちらを採用するか未決定。**
> タブ型を採用するなら S-01〜S-07 の大半は不要になる。
>
> なお `design_input_tab.md` と `design_saved_tab.md` は OneDrive 側に存在するが
> 今回の取り込み対象に含まれていない（S-08 / S-10 の詳細設計が未取り込み）。
>
> **判断材料:** 前身の実装（[reference/legacy-web/](../../../reference/legacy-web/README.md)）は
> **タブ型で実装されている**。`index.html` に「データ入力・解析 / 保存済み銘柄 / 評価基準」の
> 3タブがあり、解析結果はタブの外側の `#resultsSection` に出す構造。
> 設計書だけでなく実装もタブ型なので、**タブ型を採るのが自然**。
> ただしタブは URL に反映されておらず（`class="active"` の付け替えのみ）、
> リロード・共有で状態が失われる。新規実装では URL に載せること
> （`frontend.md`「URL に置けるものは URL に置く」）。

> S-05 は認証を導入する場合のみ。要否は `../api/auth-api.md` で決める。

## 遷移図

```
        ┌──────────────┐
        │  S-01 ダッシュ  │◀────────────┐
        └──────┬───────┘             │
               │                     │
      ┌────────┼────────┐            │
      ▼        ▼        ▼            │
  ┌────────┐ ┌──────┐ ┌────────┐    │
  │S-02    │ │S-04  │ │S-06    │    │
  │スクリーナ│ │ウォッチ│ │設定    │    │
  └───┬────┘ └──┬───┘ └────────┘    │
      │         │                    │
      └────┬────┘                    │
           ▼                         │
     ┌───────────┐                   │
     │S-03 銘柄詳細│───────────────────┘
     └───────────┘
```

- S-05（ログイン）は認証導入時のみ。未認証で S-01〜S-04 にアクセスしたら S-05 へ

## 全画面共通の要件

- ヘッダーにデータの**最終取得時刻**を表示する
- フッターに免責文言を常時表示する（投資助言ではない旨）
- スクリーニング条件・ソート順・ページ番号は **URL クエリに持つ**
  （リロード・共有・戻るボタンで壊れないこと）
- データ取得中はスケルトンを出す。レイアウトが飛ばないこと
- エラー時は「何が起きたか」と「次に何をすればよいか」を出す

## レスポンシブ

| ブレークポイント        | 対応                       |
| ----------------------- | -------------------------- |
| モバイル（〜768px）     | 一覧はカード表示に切り替え |
| デスクトップ（769px〜） | 一覧はテーブル表示         |

<!-- 要決定: モバイル対応を MVP に含めるか -->

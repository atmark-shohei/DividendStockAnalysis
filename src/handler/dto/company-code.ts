/**
 * 銘柄コードの形式。パスパラメータ・リクエストボディの両方で使う共通定義
 * （`app.ts` から切り出し。`addHoldingRequest` が同じ形式検証を必要とするため複製しない。
 * 実装計画 §6-d）。
 *
 * 4文字固定。先頭3文字は数字、末尾1文字は数字または英大文字（例: `130A`）。
 * JPX が 2024 年以降に採番している英字混じりコードに対応する。
 */
export const COMPANY_CODE_PATTERN = /^\d{3}[0-9A-Z]$/;
